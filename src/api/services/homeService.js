


/*
const axios = require("axios");

const homeAPI = axios.create({
    baseURL: "http://localhost:3000/api/pets",
});

function getPets() {
    const response = homeAPI.get("/");
    console.log(response.data);
    return response.data;
};

getPets();

module.exports = getPets;
*/
